<UICore:BasePage x:TypeArguments="Core:TestInfoPageViewModel" x:Class="Testinator.Server.DemoInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Testinator.Server"
             xmlns:Core="clr-namespace:Testinator.Server.Domain;assembly=Testinator.Server.Domain"
             xmlns:UICore="clr-namespace:Testinator.UICore;assembly=Testinator.UICore"
             mc:Ignorable="d">
    
    <Grid Background="Green">
        <StackPanel>
            <TextBox Tag="Nazwa" Text="{Binding Name.Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TestEditorInputField}" />
            <TextBlock Text="{Binding Name.ErrorMessage}" Foreground="Black" FontFamily="Bold"/>
            <Button Content="Submit" Command="{Binding SubmitCommand}" Style="{StaticResource BaseUIButton}" />
        </StackPanel>
    </Grid>
    
</UICore:BasePage>
